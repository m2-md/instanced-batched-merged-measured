{
  "name": "instanced-batched-merged-measured",
  "version": "1.0.0",
  "description": "Aynı seviye dolgusu (40 prop tipi, 1200 örnek) dört inşa yoluyla kurulup ölçülüyor: naif Mesh, InstancedMesh, BatchedMesh, mergeGeometries",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vite-node bench/build-bench.ts"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/three": "^0.185.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
